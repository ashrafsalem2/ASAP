/**
 * Production settings. The client is served from the API's own origin, so requests are relative
 * and the browser never treats them as cross-origin.
 */
export const environment = {
  production: true,
  apiBaseUrl: '',
};
